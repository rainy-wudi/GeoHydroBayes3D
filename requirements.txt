numpy>=1.23
pandas>=1.5
scipy>=1.9
scikit-learn>=1.2
matplotlib>=3.6
PyYAML>=6.0
joblib>=1.2
torch>=2.0
# Optional; the code falls back to a simple random-walk sampler if emcee is unavailable.
emcee>=3.1
